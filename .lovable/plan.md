Plan para traer el código del repo GitHub `pod-elegance` al proyecto Lovable actual

1. Extraer el listado de archivos del repositorio en GitHub.
2. Descargar los archivos fuente del repo (src/, public/, configuraciones de root).
3. Reemplazar o fusionar los archivos en el proyecto actual, respetando la estructura de TanStack Start.
4. Instalar dependencias si el repo GitHub tiene distintas versiones o paquetes extra.
5. Correr build/dev para verificar que todo compila sin errores.
6. Revisar el preview para confirmar que se ve igual al repo original.

Nota: este proceso borra el placeholder actual del index y reemplaza el código con el del repo. Una vez copiado, podemos seguir haciendo nuevos cambios sobre esa base.

¿Confirmás que arranque con esto?